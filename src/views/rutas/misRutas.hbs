
<div class="row">
  {{#each rutasdb}}
  <div class="col-md-3">
    <div class="card">
      <div class="card-body">
        <h4 class="card-title d-flex justify-content-between align-items-center">
          {{ nombre }} <a href="/rutas/editarRutas/{{_id}}"><i class="fas fa-edit"></i></a>
        </h4>
        <!-- 
          <p>{{ distancia }}</p>
          <p>{{ tiempo }}</p>
        -->
        <!-- DELETE REQUEST -->
        

        <form action="/user/amigos" method="GET">      
          <button  type="submit" class="btn btn-primary btn-block btn-sm">
            Compartir
          </button>
        </form>

        <BR>

        <form action="/rutas/eliminar/{{_id}}?_method=DELETE" method="POST">
          <input type="hidden" name="_method" value="DELETE">
          <button type="submit" class="btn btn-danger btn-block btn-sm">Eliminar</button>
        </form>
    
      </div>
    </div>
  </div>


  {{else}}
    <div class="card mx-auto">
      <div class="card-body">
        <h1>Hola {{user.nombre}}</h1>
        <p clsss="lead">Aún no tienes rutas</p>
        <form action="/rutas/nueva-ruta" method="GET">
            <button type="submit" class="btn btn-success btn-block btn-sm">Crear nueva Ruta</button>
        </form>
      </div>
    </div>
  {{/each}}

  {{#if rutasdb}}
    <div class="col-md-3">
      <div class="card">
        <div class="card-body">
          <form action="/rutas/nueva-ruta" method="GET">
            <button type="submit" class="btn btn-success btn-block btn-sm">Crear nueva Ruta</button>
          </form>
        </div>
      </div>
    </div>
  {{/if}}  

</div>



